import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  radioGroup: {
    color:"white",
    display: 'flex',
    justifyContent: 'center',
    marginBottom: '-10px',
  },
  button: {
    color:"white",
    marginTop: '20px',
  },
}));
